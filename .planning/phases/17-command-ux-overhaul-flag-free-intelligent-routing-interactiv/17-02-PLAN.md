---
phase: 17-command-ux-overhaul-flag-free-intelligent-routing-interactiv
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/opencode-rms-reviewer.md
  - src/templates/opencode-rms-validator.md
  - src/templates/opencode-rms-writer.md
  - src/templates/cursor-rms-reviewer/SKILL.md
  - src/templates/cursor-rms-validator/SKILL.md
  - src/templates/cursor-rms-writer/SKILL.md
  - src/templates/opencode-review.md
  - src/templates/opencode-settings.md
  - src/templates/cursor-rms-review/SKILL.md
  - src/templates/cursor-rms-settings/SKILL.md
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "Six new/updated template files exist: 3 OpenCode + 3 Cursor for reviewer/validator/writer commands"
    - "Each OpenCode template uses subtask: true and !rms <agent> with no arguments or flags"
    - "Each Cursor skill runs rms <agent> in the terminal and guides the user through the interactive picker"
    - "opencode-review.md argument-hint is updated to remove flag references (no --focus)"
    - "opencode-settings.md description is updated to reflect overview+reset only (no model picker)"
    - "cursor-rms-settings/SKILL.md removes references to interactive picker / model selection"
    - "cursor-rms-review/SKILL.md removes --focus guidance from the argument hint"
  artifacts:
    - path: "src/templates/opencode-rms-reviewer.md"
      provides: "OpenCode /rms-reviewer command"
    - path: "src/templates/opencode-rms-validator.md"
      provides: "OpenCode /rms-validator command"
    - path: "src/templates/opencode-rms-writer.md"
      provides: "OpenCode /rms-writer command"
    - path: "src/templates/cursor-rms-reviewer/SKILL.md"
      provides: "Cursor /rms-reviewer skill"
    - path: "src/templates/cursor-rms-validator/SKILL.md"
      provides: "Cursor /rms-validator skill"
    - path: "src/templates/cursor-rms-writer/SKILL.md"
      provides: "Cursor /rms-writer skill"
  key_links:
    - from: "src/templates/opencode-rms-reviewer.md"
      to: "rms reviewer"
      via: "!rms reviewer injection"
      pattern: "!rms reviewer"
    - from: "src/templates/cursor-rms-reviewer/SKILL.md"
      to: "rms reviewer"
      via: "terminal command"
      pattern: "rms reviewer"
---

<objective>
Create all 6 new command template files and update 4 existing templates for the flag-free UX.

Purpose: Templates are the editor-facing surface of rms commands. These files are copied verbatim by the installer into ~/.config/opencode/command/ and ~/.cursor/skills/. Getting templates right is prerequisite for Plan 03's installer wiring.

Output: 6 new template files (3 OpenCode .md + 3 Cursor SKILL.md), 4 updated existing templates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-command-ux-overhaul-flag-free-intelligent-routing-interactiv/CONTEXT.md

<interfaces>
<!-- OpenCode template format (from existing opencode-review.md) -->
---
description: "..."
argument-hint: "[optional hint]"
subtask: true
---
!rms <subcommand> $ARGUMENTS

<!-- Cursor SKILL.md format (from existing cursor-rms-review/SKILL.md) -->
---
name: rms-<name>
description: "..."
---
[Instructions for the AI agent to run terminal command and present output]

<!-- Available CLI subcommands after Phase 17: -->
rms reviewer    ← new (opens model picker for reviewer)
rms validator   ← new (opens model picker for validator)
rms writer      ← new (opens model picker for writer)
rms review      ← existing (interactive scope selection)
rms settings    ← existing (overview + reset only)
rms fix         ← existing (unchanged)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 new OpenCode templates (reviewer, validator, writer)</name>
  <files>
    src/templates/opencode-rms-reviewer.md,
    src/templates/opencode-rms-validator.md,
    src/templates/opencode-rms-writer.md
  </files>
  <action>
Create three new OpenCode command template files. Each is a thin `subtask: true` wrapper that invokes `rms <agent>` with no arguments — the interactive picker runs inside the subagent's shell context.

**src/templates/opencode-rms-reviewer.md:**
```markdown
---
description: "Set the reviewer agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
subtask: true
---
!rms reviewer
```

**src/templates/opencode-rms-validator.md:**
```markdown
---
description: "Set the validator agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
subtask: true
---
!rms validator
```

**src/templates/opencode-rms-writer.md:**
```markdown
---
description: "Set the writer agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
subtask: true
---
!rms writer
```

No argument-hint needed — these commands take no arguments (fully interactive).
  </action>
  <verify>
    <automated>ls /Users/tylercrawford/dev/github/review-my-shit/src/templates/opencode-rms-reviewer.md /Users/tylercrawford/dev/github/review-my-shit/src/templates/opencode-rms-validator.md /Users/tylercrawford/dev/github/review-my-shit/src/templates/opencode-rms-writer.md</automated>
  </verify>
  <done>All 3 OpenCode template files exist with correct subtask: true frontmatter and !rms <agent> injection</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 Cursor skill directories + update 4 existing templates</name>
  <files>
    src/templates/cursor-rms-reviewer/SKILL.md,
    src/templates/cursor-rms-validator/SKILL.md,
    src/templates/cursor-rms-writer/SKILL.md,
    src/templates/opencode-review.md,
    src/templates/opencode-settings.md,
    src/templates/cursor-rms-review/SKILL.md,
    src/templates/cursor-rms-settings/SKILL.md
  </files>
  <action>
**New Cursor skill directories:**

Create `src/templates/cursor-rms-reviewer/SKILL.md`:
```markdown
---
name: rms-reviewer
description: "Set the reviewer agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
---
Run the following command in the terminal:

```
rms reviewer
```

The command opens an interactive model picker. Guide the user through the selection:
- **max** — Best available model (highest capability, highest cost)
- **high** — High capability model
- **medium** — Balanced capability and cost
- **low** — Fast and lightweight
- **Enter custom model…** — Enter a raw model ID (e.g., `github-copilot/my-model`)

After the selection, confirm the saved model to the user.
Config is saved to ~/.config/rms/config.json.

If the command fails with a TTY error, instruct the user to run `rms settings --reviewer <spec>` in a real terminal instead.
```

Create `src/templates/cursor-rms-validator/SKILL.md`:
```markdown
---
name: rms-validator
description: "Set the validator agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
---
Run the following command in the terminal:

```
rms validator
```

The command opens an interactive model picker. Guide the user through the selection:
- **max** — Best available model (highest capability, highest cost)
- **high** — High capability model
- **medium** — Balanced capability and cost
- **low** — Fast and lightweight
- **Enter custom model…** — Enter a raw model ID (e.g., `github-copilot/my-model`)

After the selection, confirm the saved model to the user.
Config is saved to ~/.config/rms/config.json.

If the command fails with a TTY error, instruct the user to run `rms settings --validator <spec>` in a real terminal instead.
```

Create `src/templates/cursor-rms-writer/SKILL.md`:
```markdown
---
name: rms-writer
description: "Set the writer agent model — opens an interactive picker with variant tiers (max/high/medium/low)"
---
Run the following command in the terminal:

```
rms writer
```

The command opens an interactive model picker. Guide the user through the selection:
- **max** — Best available model (highest capability, highest cost)
- **high** — High capability model
- **medium** — Balanced capability and cost
- **low** — Fast and lightweight
- **Enter custom model…** — Enter a raw model ID (e.g., `github-copilot/my-model`)

After the selection, confirm the saved model to the user.
Config is saved to ~/.config/rms/config.json.

If the command fails with a TTY error, instruct the user to run `rms settings --writer <spec>` in a real terminal instead.
```

**Updated existing templates:**

Update `src/templates/opencode-review.md` — remove flag references from argument-hint:
```markdown
---
description: "Run rms code review — prompts for scope (local diff, PR, or full codebase)"
subtask: true
---
!rms review $ARGUMENTS
```
(argument-hint removed since no flags are expected from editor commands per D-1)

Update `src/templates/opencode-settings.md` — reflect overview+reset only (no model picker language):
```markdown
---
description: "View current rms model config and reset to defaults. Use /rms-reviewer, /rms-validator, or /rms-writer to change models."
subtask: true
---
!rms settings $ARGUMENTS
```

Update `src/templates/cursor-rms-review/SKILL.md` — remove --focus flag reference from argument-hint; keep the scope routing logic:
```markdown
---
name: rms-review
description: "Run rms code review — prompts for scope (local diff, PR, or full codebase)"
---
Run the following command in the terminal and present the output to the user:

```
rms review $ARGUMENTS
```

If the output contains 'What would you like to review?', present the options to the user and ask for their choice. Then re-invoke with their selection:
- If they choose local: `rms review local`
- If they choose PR: ask for the PR number, then run `rms review pr <number>`
- If they choose full: `rms review full`

After a successful review, read REPORT.md inside `.reviews/<session-id>/` and present findings grouped by severity (critical → high → medium → low → info). Include the session ID so the user can reference findings with `/rms-fix`.

PR reviews require `GITHUB_TOKEN` to be set. If the command fails with an auth error, remind the user to export their GitHub token.
```

Update `src/templates/cursor-rms-settings/SKILL.md` — simplify to overview+reset only, remove model picker / --reviewer/--validator/--writer flag sections (those are now /rms-reviewer etc.):
```markdown
---
name: rms-settings
description: "View current rms model config (~/.config/rms/config.json) and reset to defaults."
---
Run the following command in the terminal and present the output to the user:

```
rms settings $ARGUMENTS
```

## Viewing current config

Run `rms settings` with no arguments to see which models are currently configured for each agent.

## Resetting to defaults

```
rms settings --reset
```

Deletes the config file and reverts to environment variable fallback (AI_SDK_PROVIDER + AI_SDK_MODEL).

## Changing individual agent models

Use the dedicated commands:
- `/rms-reviewer` — Set reviewer model
- `/rms-validator` — Set validator model
- `/rms-writer` — Set writer model

Or use the CLI flags for scripting:
```
rms settings --reviewer copilot:claude-opus-4-5
rms settings --validator copilot:github-copilot/gpt-5.4
rms settings --writer openai:gpt-4o
```

## Supported providers

| Provider | Requires |
|----------|---------|
| `copilot` | `GITHUB_TOKEN` or opencode auth |
| `anthropic` | `ANTHROPIC_API_KEY` |
| `openai` | `OPENAI_API_KEY` |
| `google` | `GOOGLE_GENERATIVE_AI_API_KEY` |
```
  </action>
  <verify>
    <automated>ls /Users/tylercrawford/dev/github/review-my-shit/src/templates/cursor-rms-reviewer/SKILL.md /Users/tylercrawford/dev/github/review-my-shit/src/templates/cursor-rms-validator/SKILL.md /Users/tylercrawford/dev/github/review-my-shit/src/templates/cursor-rms-writer/SKILL.md</automated>
  </verify>
  <done>All 3 new Cursor skill SKILL.md files exist; 4 existing templates updated with flag-free descriptions and accurate command guidance</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template content → editor | Templates are static markdown files; no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Spoofing | OpenCode subtask: true templates | accept | subtask: true ensures mechanical isolation; the !rms injection runs in a clean context with no session history |
</threat_model>

<verification>
- All 6 new template files exist (3 opencode-rms-*.md + 3 cursor-rms-*/SKILL.md)
- opencode-rms-*.md files all have `subtask: true` and `!rms <agent>` injection
- Cursor SKILL.md files have correct `name:` frontmatter matching the target skill dir name
- opencode-review.md no longer has argument-hint with flags
- cursor-rms-settings/SKILL.md no longer references model picker or --reviewer/--validator/--writer flags
</verification>

<success_criteria>
- 6 new template files exist and are well-formed markdown
- 4 existing templates updated to reflect flag-free UX and clear division of responsibilities
- All cursor SKILL.md files have `name:` frontmatter that matches their directory name (rms-reviewer, rms-validator, rms-writer)
</success_criteria>

<output>
After completion, create `.planning/phases/17-command-ux-overhaul-flag-free-intelligent-routing-interactiv/17-02-SUMMARY.md`
</output>
