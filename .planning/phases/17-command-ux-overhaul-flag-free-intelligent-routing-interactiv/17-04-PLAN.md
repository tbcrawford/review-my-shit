---
phase: 17-command-ux-overhaul-flag-free-intelligent-routing-interactiv
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified:
  - src/installer.test.ts
  - src/index.test.ts
  - src/setup.test.ts
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06]

must_haves:
  truths:
    - "All existing vitest tests still pass (no regressions)"
    - "Installer test validates 6 OpenCode + 6 Cursor templates are written (was 3+3)"
    - "Index test validates rms reviewer/validator/writer subcommands are registered"
    - "Setup test validates completion message lists all 6 /rms-* commands"
    - "Build succeeds (bun run build or npm run build exits 0)"
    - "User confirms /rms-reviewer, /rms-validator, /rms-writer are visible in editor command palette"
  artifacts:
    - path: "src/installer.test.ts"
      provides: "Updated test validating 6 installed commands"
    - path: "src/index.test.ts"
      provides: "Tests for rms reviewer/validator/writer registration"
    - path: "src/setup.test.ts"
      provides: "Updated completion message test"
    - path: "dist/"
      provides: "Built output including all 6 new templates"
  key_links:
    - from: "src/installer.test.ts"
      to: "src/installer.ts"
      via: "vitest import + GLOBAL_INSTALLS / CURSOR_SKILL_INSTALLS count assertions"
      pattern: "GLOBAL_INSTALLS"
    - from: "dist/templates/"
      to: "src/templates/"
      via: "build script cp"
      pattern: "templates"
---

<objective>
Update tests for the installer, index, and setup changes. Run the full test suite. Build and verify. Human checkpoint to confirm editor commands are visible.

Purpose: Final verification wave. Tests catch regressions and validate counts. The build confirms templates are correctly copied. The human checkpoint confirms the editor UX is working end-to-end.

Output: Passing test suite, clean build, human confirmation that new editor commands are discoverable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-command-ux-overhaul-flag-free-intelligent-routing-interactiv/CONTEXT.md
@.planning/phases/17-command-ux-overhaul-flag-free-intelligent-routing-interactiv/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update installer.test.ts, index.test.ts, setup.test.ts for Phase 17 changes</name>
  <files>src/installer.test.ts, src/index.test.ts, src/setup.test.ts</files>
  <behavior>
    Installer test:
    - install() with tmpDir writes 6 files to OpenCode dir (was 3): rms-review.md, rms-fix.md, rms-settings.md, rms-reviewer.md, rms-validator.md, rms-writer.md
    - install() writes 6 Cursor skills (was 3): rms-review, rms-fix, rms-settings, rms-reviewer, rms-validator, rms-writer
    - install({ editors: ['opencode'] }) writes 6 OpenCode files, 0 Cursor files
    - install({ editors: ['cursor'] }) writes 0 OpenCode files, 6 Cursor skill dirs

    Index test:
    - program.commands map includes 'reviewer', 'validator', 'writer' (verify via program.commands.map(c => c.name()))
    - 'settings' command description does NOT mention 'interactive' or 'picker' (it's overview-only now)

    Setup test:
    - BANNER_STRING export is unchanged (regression guard)
    - resolveEditorsFromArgs behavior unchanged (no changes to that function)
    - Completion log output after install() includes '/rms-reviewer', '/rms-validator', '/rms-writer' (check that the new commands appear in what's console.log'd)
  </behavior>
  <action>
Read the existing test files first, then make targeted additions/updates:

**src/installer.test.ts** — Update the counts and add assertions for the 3 new command names:
- Find the test that asserts installed file count (e.g., `expect(files.length).toBe(3)`) and update to `toBe(6)`
- Add assertions that rms-reviewer.md, rms-validator.md, rms-writer.md exist in the OpenCode dir
- Add assertions that rms-reviewer, rms-validator, rms-writer skill dirs exist in the Cursor dir
- If the test uses a fake TEMPLATES_DIR pointed at src/templates/, the new template files created in Plan 02 will be found automatically — no stub needed

**src/index.test.ts** — Add tests for new subcommands:
- Import the program (if it's not already imported/tested); or add a test that calls `program.commands.map(c => c.name())` and checks it includes 'reviewer', 'validator', 'writer'
- Verify that 'settings' command description includes 'overview' or 'config' but NOT 'interactive' or 'picker'
- Existing tests (review, fix, etc.) must still pass — do not break them

**src/setup.test.ts** — Add/update the completion message test:
- If there's a test that checks console.log output from main(), add assertions that '/rms-reviewer', '/rms-validator', '/rms-writer' appear in the output
- If no such test exists, add a smoke test: mock install() to be a no-op, mock ensureDefaultConfig(), call main() with -y flag (to skip the interactive prompt), capture stdout, assert it includes '/rms-reviewer'

After edits, run: `npx vitest run 2>&1 | tail -30`

Fix any test failures before declaring done.
  </action>
  <verify>
    <automated>cd /Users/tylercrawford/dev/github/review-my-shit && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>All vitest tests pass with exit code 0; new test assertions for 6-command install and new subcommands are in place</done>
</task>

<task type="auto">
  <name>Task 2: Build and run install</name>
  <files>dist/ (generated)</files>
  <action>
1. Run the build:
```
npm run build
```
or if the project uses bun:
```
bun run build
```
(Check package.json scripts for the correct build command.)

Verify build exits 0.

2. Verify dist/templates/ contains the new template files:
```
ls dist/templates/opencode-rms-reviewer.md dist/templates/opencode-rms-validator.md dist/templates/opencode-rms-writer.md
ls dist/templates/cursor-rms-reviewer/SKILL.md dist/templates/cursor-rms-validator/SKILL.md dist/templates/cursor-rms-writer/SKILL.md
```

3. Run `rms install --yes` (using the locally built binary or `node dist/index.js install --yes`) to install all 6 commands globally:
```
node dist/index.js install --yes
```

Confirm output shows all 6 commands in completion message and the default config message (either "Default config created" or no message if config already exists).

4. Confirm the installed files exist in ~/.config/opencode/command/:
```
ls ~/.config/opencode/command/rms-reviewer.md ~/.config/opencode/command/rms-validator.md ~/.config/opencode/command/rms-writer.md
```

5. Confirm the Cursor skills exist in ~/.cursor/skills/:
```
ls ~/.cursor/skills/rms-reviewer/SKILL.md ~/.cursor/skills/rms-validator/SKILL.md ~/.cursor/skills/rms-writer/SKILL.md
```
  </action>
  <verify>
    <automated>cd /Users/tylercrawford/dev/github/review-my-shit && npm run build 2>&1 | tail -10 && ls dist/templates/opencode-rms-reviewer.md dist/templates/cursor-rms-reviewer/SKILL.md 2>&1</automated>
  </verify>
  <done>Build exits 0; dist/templates/ contains all 6 new template files; rms install --yes completes with 6-command completion message</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Three new editor commands installed globally:
- /rms-reviewer — set reviewer agent model via interactive picker
- /rms-validator — set validator agent model via interactive picker  
- /rms-writer — set writer agent model via interactive picker

Updated commands:
- /rms-settings — now shows config overview only (no model picker)
- /rms-review — unchanged behavior, cleaner description (no flag references)

Default config created at ~/.config/rms/config.json if it didn't exist.
  </what-built>
  <how-to-verify>
1. **Restart your editor** (OpenCode or Cursor) to pick up the new commands.

2. **Test /rms-reviewer in OpenCode:**
   - Open the command palette, type "rms-reviewer"
   - Confirm the command appears with description "Set the reviewer agent model"
   - Invoke it — confirm it opens an interactive picker showing: max, high, medium, low, Enter custom model…
   - Select "high" — confirm it saves and prints the confirmation

3. **Test /rms-settings:**
   - Invoke /rms-settings with no arguments
   - Confirm it shows the current config overview (reviewer/validator/writer with their models)
   - Confirm it does NOT open a model picker (that's moved to /rms-reviewer etc.)

4. **Test default config (if this is a fresh install):**
   - Run `cat ~/.config/rms/config.json` in terminal
   - Confirm it contains the three-agent default config with copilot provider

5. **(Optional) Test /rms-validator and /rms-writer** the same way as step 2.
  </how-to-verify>
  <resume-signal>Type "approved" if all commands work as described, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/ build output → editor | Built templates are static markdown; no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Repudiation | build + install | accept | All installs are idempotent overwrites; no audit trail needed for dev tooling |
</threat_model>

<verification>
- `npx vitest run` passes with exit code 0 (all tests, including updated ones)
- `npm run build` exits 0
- dist/templates/ contains all 6 new template files
- ~/.config/opencode/command/ contains rms-reviewer.md, rms-validator.md, rms-writer.md
- ~/.cursor/skills/ contains rms-reviewer/, rms-validator/, rms-writer/ skill directories
- Human checkpoint approved
</verification>

<success_criteria>
- Full test suite passes (0 failures, no regressions)
- Build succeeds and new templates are in dist/templates/
- rms install --yes installs 6 commands per editor and creates default config
- Human confirms /rms-reviewer, /rms-validator, /rms-writer are discoverable in editor and the picker UX works
</success_criteria>

<output>
After completion, create `.planning/phases/17-command-ux-overhaul-flag-free-intelligent-routing-interactiv/17-04-SUMMARY.md`
</output>
