---
phase: "12"
plan: "03"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - package.json
  - src/setup.ts
  - src/index.ts
  - README.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "package.json version is 0.3.0"
    - "src/setup.ts banner shows v0.3.0"
    - "program.version() in src/index.ts shows 0.3.0"
    - "README Install section documents --opencode, --cursor, --yes/-y flags for both npx review-my-shit and rms install"
    - "All tests pass with 0 failures"
    - "Human confirms npx entrypoint shows banner, flags work, and completion output is polished"
  artifacts:
    - path: "package.json"
      provides: "version bump to 0.3.0"
      contains: '"version": "0.3.0"'
    - path: "README.md"
      provides: "updated install section with flag docs"
      contains: "npx review-my-shit --opencode"
  key_links:
    - from: "package.json version 0.3.0"
      to: "src/setup.ts VERSION constant"
      via: "manual sync — both must be 0.3.0"
      pattern: "0.3.0"
    - from: "src/index.ts program.version()"
      to: "package.json version"
      via: "manual sync — both must be 0.3.0"
      pattern: "version.*0.3.0"
---

<objective>
Version bump 0.2.0 → 0.3.0 across package.json, src/setup.ts banner, and src/index.ts. Update README Install section to document the new CLI flags. Verify full test suite. Human checkpoint to confirm UX end-to-end.

Purpose: Sync version strings across the codebase and document the new flags for users. This is a meaningful UX release — 0.3.0 is the appropriate version bump.
Output: Updated `package.json`, `src/setup.ts` (VERSION constant), `src/index.ts` (program.version()), `README.md` (Install flags section).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-overhaul-installer-ux-with-interactive-editor-picker-cli-flags-for-scripting-rms-banner-and-polished-output/12-RESEARCH.md
</context>

<interfaces>
<!-- Current version locations that need updating -->
<!-- 1. package.json line 4: "version": "0.2.0" -->
<!-- 2. src/setup.ts: const VERSION = '0.2.0'; -->
<!-- 3. src/index.ts: program.version('0.2.0') -->

<!-- README Install section (current — lines 49-63) — will be replaced -->
```markdown
### Install

```bash
npx review-my-shit
```

Running `npx review-my-shit` prompts you to choose which editors to install for (OpenCode, Cursor, or both), then writes the slash command files:

- **OpenCode:** `~/.config/opencode/command/` — global, available in all projects
- **Cursor:** `~/.cursor/skills/<name>/SKILL.md` — global, available in all projects

Re-run after upgrading to pick up updated commands.

> **Already have rms installed globally?** You can also run `rms install` directly.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.3.0 across package.json, src/setup.ts, and src/index.ts</name>
  <files>package.json, src/setup.ts, src/index.ts</files>
  <read_first>
    - package.json — confirm current version is "0.2.0" before changing
    - src/setup.ts — find the `const VERSION = '0.2.0'` line; change to `'0.3.0'`
    - src/index.ts — find the `.version('0.2.0')` call on the Commander program; change to `'0.3.0'`
  </read_first>
  <action>
Make exactly three targeted string replacements:

**1. package.json** — change line 4:
```json
  "version": "0.2.0",
```
→
```json
  "version": "0.3.0",
```

**2. src/setup.ts** — change the VERSION constant:
```typescript
const VERSION = '0.2.0';
```
→
```typescript
const VERSION = '0.3.0';
```

**3. src/index.ts** — change the program version:
```typescript
  .version('0.2.0');
```
→
```typescript
  .version('0.3.0');
```

After all three replacements, run:
```bash
npm run build 2>&1 | tail -5
npm test 2>&1 | tail -8
```
Both must exit 0.

**Verify all three are in sync:**
```bash
grep '"version"' package.json
grep 'VERSION' src/setup.ts
grep 'version(' src/index.ts
```
All three should show `0.3.0`.
  </action>
  <verify>
    <automated>grep '"version": "0.3.0"' package.json && grep "VERSION = '0.3.0'" src/setup.ts && grep "version('0.3.0')" src/index.ts && echo "PASS: all three version strings are 0.3.0"</automated>
  </verify>
  <done>
    - `package.json` contains `"version": "0.3.0"`
    - `src/setup.ts` contains `const VERSION = '0.3.0'`
    - `src/index.ts` contains `.version('0.3.0')`
    - `npm run build` exits 0
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README Install section with new flag documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md — read the full "### Install" subsection (lines ~49-63) to understand what to replace. Do NOT change any other README section.
  </read_first>
  <action>
Find the current `### Install` section in README.md and replace it entirely with the following new content:

**Find (the entire Install section including code fence):**
```markdown
### Install

```bash
npx review-my-shit
```

Running `npx review-my-shit` prompts you to choose which editors to install for (OpenCode, Cursor, or both), then writes the slash command files:

- **OpenCode:** `~/.config/opencode/command/` — global, available in all projects
- **Cursor:** `~/.cursor/skills/<name>/SKILL.md` — global, available in all projects

Re-run after upgrading to pick up updated commands.

> **Already have rms installed globally?** You can also run `rms install` directly.
```

**Replace with:**
```markdown
### Install

```bash
npx review-my-shit
```

Running `npx review-my-shit` prompts you to choose which editors to install for (OpenCode, Cursor, or both), then writes the slash command files:

- **OpenCode:** `~/.config/opencode/command/` — global, available in all projects
- **Cursor:** `~/.cursor/skills/<name>/SKILL.md` — global, available in all projects

Re-run after upgrading to pick up updated commands.

**Scripting / CI:** Pass flags to skip the interactive prompt:

```bash
npx review-my-shit --opencode   # OpenCode only
npx review-my-shit --cursor     # Cursor only
npx review-my-shit --yes        # Both editors (non-interactive)
```

> **Already have rms installed globally?** You can also run `rms install` directly, with the same flags: `rms install --opencode`, `rms install --cursor`, `rms install -y`.
```

Do NOT change any other section of README.md.

After editing, verify:
```bash
grep "npx review-my-shit --opencode" README.md
grep "rms install --opencode" README.md
```
Both should find a match.
  </action>
  <verify>
    <automated>grep -c "npx review-my-shit --opencode" README.md && grep -c "rms install --opencode" README.md && echo "PASS: README has flag docs"</automated>
  </verify>
  <done>
    - README.md `### Install` section includes `npx review-my-shit --opencode`, `--cursor`, `--yes` examples
    - README.md includes note about `rms install` accepting the same flags
    - No other README sections changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Phase 12 UX end-to-end</name>
  <files>dist/setup.js, dist/index.js</files>
  <action>Human verification of all Phase 12 changes. Run the checks below and confirm all pass before approving.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm test 2>&1 | tail -5 && echo "BUILD + TESTS: OK"</automated>
  </verify>
  <done>Human confirms: banner displays, flags work in both entrypoints, output is polished, backward compat preserved.</done>
  <what-built>
    - **Plan 01:** `src/setup.ts` — ASCII banner, CLI flags (--opencode/--cursor/--yes/-y), improved prompt with paths, polished completion output
    - **Plan 02:** `src/installer.ts` — consistent `  OpenCode  path` + `    ✓ file` formatting; `src/index.ts` — install command with --opencode/--cursor/-y/--yes flags + completion summary
    - **Plan 03:** Version bumped 0.2.0 → 0.3.0 in package.json + setup.ts + index.ts; README Install section updated with flag docs
  </what-built>
  <how-to-verify>
    1. **Build and tests:**
       ```bash
       npm run build && npm test
       ```
       Expected: build exits 0, ≥161 tests pass, 0 failures.

    2. **Banner appears:**
       ```bash
       echo "" | node dist/setup.js
       ```
       Expected: ASCII banner shows `rms — review-my-shit  v0.3.0` (even when immediately pressing Enter).

    3. **--opencode flag (scripting path, no prompt):**
       ```bash
       node dist/setup.js --opencode
       ```
       Expected: Banner appears, "Installing for OpenCode..." printed, OpenCode files written, no interactive prompt shown, "✓ Done." message with commands.

    4. **--cursor flag:**
       ```bash
       node dist/setup.js --cursor
       ```
       Expected: Banner, "Installing for Cursor...", Cursor skills written, no prompt. "✓ Done." with Cursor commands.

    5. **--yes flag (both editors, no prompt):**
       ```bash
       node dist/setup.js --yes
       ```
       Expected: Banner, "Installing for OpenCode + Cursor...", both written, "✓ Done." with both editors' commands.

    6. **Interactive path (no flags):**
       ```bash
       node dist/setup.js
       ```
       Expected: Banner, interactive prompt shows options with destination paths (e.g., `~/.config/opencode/command/`). Enter "1" → installs OpenCode only.

    7. **rms install flags:**
       ```bash
       node dist/index.js install --opencode
       ```
       Expected: OpenCode files written, "✓ rms installed for OpenCode." completion message.
       ```bash
       node dist/index.js install --cursor
       ```
       Expected: Cursor files written, "✓ rms installed for Cursor." completion message.

    8. **rms install backward compat (no flags):**
       ```bash
       node dist/index.js install
       ```
       Expected: Both editors installed, "✓ rms installed for OpenCode + Cursor." — same as before Phase 12.

    9. **Version check:**
       ```bash
       node dist/index.js --version
       ```
       Expected: `0.3.0`

    10. **README flag docs:**
        ```bash
        grep "npx review-my-shit --opencode" README.md
        grep "rms install --opencode" README.md
        ```
        Expected: Both lines found.
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
```bash
# Version sync check
grep '"version": "0.3.0"' package.json && echo "package.json: 0.3.0 ✓"
grep "VERSION = '0.3.0'" src/setup.ts && echo "setup.ts: 0.3.0 ✓"
grep "version('0.3.0')" src/index.ts && echo "index.ts: 0.3.0 ✓"

# README updated
grep "npx review-my-shit --opencode" README.md && echo "README: npx flags ✓"
grep "rms install --opencode" README.md && echo "README: rms flags ✓"

# Build succeeds
npm run build 2>&1 | tail -3

# All tests pass
npm test 2>&1 | tail -5
```
</verification>

<success_criteria>
- `package.json`, `src/setup.ts` VERSION, and `src/index.ts` program.version() all read `0.3.0`
- `node dist/index.js --version` outputs `0.3.0`
- `node dist/setup.js --help` (if any) or banner shows `v0.3.0`
- README Install section documents `--opencode`, `--cursor`, `--yes` for both `npx review-my-shit` and `rms install`
- All tests pass (≥161, 0 failures)
- Human checkpoint approved: banner shows on startup, all flags work in both entrypoints, backward compat preserved, output is polished
</success_criteria>

<output>
After completion, create `.planning/phases/12-overhaul-installer-ux-with-interactive-editor-picker-cli-flags-for-scripting-rms-banner-and-polished-output/12-03-SUMMARY.md`
</output>
