---
phase: 10-change-the-installation-of-this-project-to-be-global-instead-of-per-project
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
files_modified:
  - README.md
  - package.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "README Getting Started section shows 'npm install -g review-my-shit' as the primary install method"
    - "README shows 'rms install' (not 'node dist/index.js install') as the install command"
    - "README explains the global/per-project split for OpenCode vs Cursor"
    - "README CLI reference uses 'rms <subcommand>' not 'node dist/index.js'"
    - "package.json version is bumped to 0.2.0"
  artifacts:
    - path: "README.md"
      provides: "Updated user-facing documentation for global install"
      contains: "npm install -g review-my-shit"
    - path: "package.json"
      provides: "Version bump for the global-install release"
      contains: '"version": "0.2.0"'
  key_links:
    - from: "README.md Getting Started"
      to: "global npm install"
      via: "npm install -g review-my-shit"
      pattern: "npm install -g review-my-shit"
---

<objective>
Update the README to document the new global install flow, replace all `node dist/index.js` CLI references with `rms`, explain the OpenCode-global / Cursor-per-project split, and bump the version to 0.2.0 to mark the global-install release.

Purpose: Users discover how to install rms from the README. The current README documents a per-project clone-and-build workflow that no longer matches the intended experience. This plan makes the documentation match reality.

Output:
- `README.md` — Getting Started shows `npm install -g review-my-shit && rms install`; CLI reference uses `rms`
- `package.json` — `"version": "0.2.0"`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-RESEARCH.md
@.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-01-SUMMARY.md
@.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-02-SUMMARY.md

<interfaces>
<!-- Current README Install section (exact content to replace) -->
Current "### Install" section in README.md:
```bash
# Clone the repo
git clone https://github.com/tylercrawford/review-my-shit
cd review-my-shit

# Install dependencies and build
npm install
npm run build

# Install slash commands into the current project
node dist/index.js install
```

Target "### Install" section:
```bash
# Install rms globally
npm install -g review-my-shit

# Install slash commands (OpenCode: global, Cursor: current project)
rms install
```

<!-- Current CLI reference section uses node dist/index.js -->
Current "## CLI reference" section:
```bash
node dist/index.js review [local | pr <pr-number>] [--focus <area>]
node dist/index.js fix [<finding-id>] [--session <id>]
node dist/index.js settings [...]
node dist/index.js install
```

Target:
```bash
rms review [local | pr <pr-number>] [--focus <area>]
rms fix [<finding-id>] [--session <id>]
rms settings [...]
rms install
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README Getting Started and CLI reference sections</name>
  <files>README.md</files>
  <read_first>
    - README.md — full file; identify: "### Install" section, "## CLI reference" section, "## Development" section
  </read_first>
  <action>
Make the following targeted changes to README.md:

**1. Update "### Install" section:**

Replace the entire install code block (clone → npm install → npm run build → node dist/index.js install) with:

```markdown
### Install

```bash
# Install rms globally
npm install -g review-my-shit

# Install slash commands
# OpenCode: installed globally (available in all projects automatically)
# Cursor: installed in the current project (run from each project where you want Cursor integration)
rms install
```

`install` writes slash command files:
- **OpenCode:** `~/.config/opencode/command/` — global, no per-project setup needed
- **Cursor:** `.cursor/commands/` in your current project — run `rms install` from each project

Re-run after upgrading.
```

**2. Update "## CLI reference" section:**

Replace all `node dist/index.js` with `rms` in the CLI reference code block:

```bash
# Unified review command (prompts for scope if none given)
rms review [local | pr <pr-number>] [--focus <area>]

# Show or apply a finding
rms fix [<finding-id>] [--session <id>]

# View or set per-agent model config
rms settings [--reviewer p:m] [--validator p:m] [--writer p:m] [--reset]

# Install slash commands
rms install
```

**3. Update "## Development" section:**

The Development section currently shows `node dist/index.js` in an implied way via the build step. Update it to explicitly distinguish global usage from development usage:

After the existing development commands (npm install, npm run build, npm test, npm run typecheck), add a note:

```markdown
For development, use `node dist/index.js <command>` instead of the globally-installed `rms` to test local changes before publishing.
```

**4. Keep unchanged:**
- The pipeline diagram
- The Getting started prerequisites section (Node.js 18+, editor, API key)
- The "Configure models" section (already uses `/rms-settings` not `node dist/index.js`)
- All command documentation sections (/rms-review, /rms-fix, /rms-settings)
- The dimensions section
- The Finding IDs section
- The Output section
  </action>
  <verify>
    <automated>
      grep 'npm install -g review-my-shit' README.md && echo "PASS: global install shown" || echo "FAIL: no global install"
      grep 'rms install' README.md && echo "PASS: rms install shown" || echo "FAIL"
      grep 'node dist/index.js install' README.md && echo "FAIL: old node dist install still present" || echo "PASS: old install removed"
      grep 'node dist/index.js review' README.md && echo "FAIL: CLI ref not updated" || echo "PASS: CLI ref uses rms"
      grep 'rms review' README.md | grep -v '/rms-review' | grep -v '^\`\`\`' && echo "PASS: rms review in CLI ref" || true
      grep '~/.config/opencode/command' README.md && echo "PASS: global opencode path documented" || echo "FAIL: path not mentioned"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `npm install -g review-my-shit`
    - README.md contains `rms install` (not `node dist/index.js install`)
    - README.md contains `~/.config/opencode/command/` explaining global OpenCode install
    - README.md contains `.cursor/commands/` explaining per-project Cursor install
    - README.md CLI reference uses `rms review`, `rms fix`, `rms settings`, `rms install`
    - No `node dist/index.js install` remains anywhere in the Getting Started or CLI reference sections
    - The Development section still shows `npm run build` and `npm test`
  </acceptance_criteria>
  <done>README updated. Getting Started shows global npm install flow. CLI reference uses rms binary.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 0.2.0</name>
  <files>package.json</files>
  <read_first>
    - package.json — current version field (should be 0.1.0 from Plan 01 work)
  </read_first>
  <action>
In `package.json`, change the `"version"` field from `"0.1.0"` to `"0.2.0"`.

Also update the version string in `src/index.ts` where `program.version('0.1.0')` is hardcoded (around line 293):
```typescript
program
  .name('rms')
  .description('review-my-shit — AI code review pipeline')
  .version('0.1.0');
```
Change to `.version('0.2.0')`.

No other changes. The package name was already changed to `review-my-shit` in Plan 01.
  </action>
  <verify>
    <automated>
      node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('version:', p.version, '| name:', p.name)" 
      # Expected: version: 0.2.0 | name: review-my-shit
      grep '"version": "0.2.0"' package.json && echo "PASS: version bumped" || echo "FAIL: version not bumped"
      grep 'version.*0.2.0' src/index.ts && echo "PASS: CLI version updated" || echo "FAIL: CLI version not updated"
      npm run build && echo "PASS: build" || echo "FAIL"
      npm test && echo "PASS: all tests" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `"version"` is `"0.2.0"`
    - `src/index.ts` `.version('0.2.0')` matches
    - `npm run build` exits 0
    - `npm test` exits 0 (all 152+ tests pass)
  </acceptance_criteria>
  <done>Version bumped to 0.2.0. Build and all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify global install end-to-end</name>
  <action>Human verifies the complete Phase 10 global install changes work correctly end-to-end: OpenCode gets global commands, Cursor gets per-project commands, templates use the rms binary, and all tests pass.</action>
  <verify>See how-to-verify steps below. All 5 steps must pass.</verify>
  <done>User types "approved" after all verification steps pass.</done>
  <what-built>
Complete Phase 10 global installation changes:
- Plan 01: Package renamed to `review-my-shit`, all 6 templates updated to call `rms` binary
- Plan 02: Installer redesigned — OpenCode → `~/.config/opencode/command/` (global), Cursor → `.cursor/commands/` (per-project)
- Plan 03 (this): README updated for global install flow, version bumped to 0.2.0
  </what-built>
  <how-to-verify>
**Step 1: Test global install locally**
```bash
npm run build
# Simulate global install by running the installer from this project:
node dist/index.js install
```
Expected output:
```
OpenCode (global): /Users/<you>/.config/opencode/command
  ✓ rms-review.md
  ✓ rms-fix.md
  ✓ rms-settings.md

Cursor (this project): /path/to/review-my-shit
  ✓ .cursor/commands/rms-review.md
  ✓ .cursor/commands/rms-fix.md
  ✓ .cursor/commands/rms-settings.md

rms installed.
  OpenCode: commands available globally in all projects.
  Cursor: commands installed in this project...
```

**Step 2: Verify global OpenCode template content**
```bash
cat ~/.config/opencode/command/rms-review.md
```
Expected: contains `!rms review $ARGUMENTS` (not `!node dist/index.js`)

**Step 3: Verify in OpenCode**
Open any OTHER project in OpenCode (not review-my-shit). Type `/rms` in the command palette. The `/rms-review`, `/rms-fix`, and `/rms-settings` commands should appear.

**Step 4: Run all tests**
```bash
npm test
```
Expected: all 152+ tests pass

**Step 5: Check package.json**
```bash
node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('name:', p.name, '| version:', p.version)"
```
Expected: `name: review-my-shit | version: 0.2.0`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Final verification after checkpoint approval:

```bash
# Package identity
node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.name, p.version)"
# Expected: review-my-shit 0.2.0

# Template integrity
grep -l 'node dist' src/templates/*.md && echo "FAIL: templates still reference node dist" || echo "PASS"

# Installer target paths
grep 'opencode.*command' src/installer.ts | head -3  # should show ~/.config/opencode/command
grep '.cursor/commands' src/installer.ts | head -3   # should show .cursor/commands

# All tests
npm test
```
</verification>

<success_criteria>
- `npm test` exits 0 (all 152+ tests pass)
- `npm run build` exits 0
- `node dist/index.js install` writes to `~/.config/opencode/command/` AND `.cursor/commands/`
- The written OpenCode templates contain `!rms` (not `!node dist/index.js`)
- `/rms-review` appears in OpenCode from a different project after install
- README shows `npm install -g review-my-shit && rms install` as primary install flow
- `package.json` name is `review-my-shit`, version is `0.2.0`
- User checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-03-SUMMARY.md`
</output>
