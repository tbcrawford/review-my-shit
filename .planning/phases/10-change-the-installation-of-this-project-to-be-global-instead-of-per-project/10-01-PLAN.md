---
phase: 10-change-the-installation-of-this-project-to-be-global-instead-of-per-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/templates/opencode-review.md
  - src/templates/opencode-rms-fix.md
  - src/templates/opencode-settings.md
  - src/templates/cursor-review.md
  - src/templates/cursor-rms-fix.md
  - src/templates/cursor-settings.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "package.json name field is 'review-my-shit' (not 'rms')"
    - "All 3 OpenCode templates call '!rms <subcommand>' not '!node dist/index.js'"
    - "All 3 Cursor templates call 'rms <subcommand>' not 'node dist/index.js'"
    - "The rms bin entry in package.json remains 'rms: dist/index.js'"
  artifacts:
    - path: "package.json"
      provides: "Correct package name for npm publish"
      contains: '"name": "review-my-shit"'
    - path: "src/templates/opencode-review.md"
      provides: "Global OpenCode review command"
      contains: "!rms review"
    - path: "src/templates/opencode-rms-fix.md"
      provides: "Global OpenCode fix command"
      contains: "!rms fix"
    - path: "src/templates/opencode-settings.md"
      provides: "Global OpenCode settings command"
      contains: "!rms settings"
    - path: "src/templates/cursor-review.md"
      provides: "Global Cursor review command"
      contains: "rms review"
    - path: "src/templates/cursor-rms-fix.md"
      provides: "Global Cursor fix command"
      contains: "rms fix"
    - path: "src/templates/cursor-settings.md"
      provides: "Global Cursor settings command"
      contains: "rms settings"
  key_links:
    - from: "package.json bin"
      to: "dist/index.js"
      via: "bin field"
      pattern: '"rms".*"dist/index.js"'
---

<objective>
Rename the npm package from `rms` to `review-my-shit` (the `rms` name is taken on npm by an unrelated package) and update all 6 editor command templates to call the globally-installed `rms` CLI binary instead of the local `node dist/index.js`.

Purpose: The package name conflict blocks npm publishing. The template update is the prerequisite for global install to work — templates must call the binary by name, not by local path.

Output:
- `package.json` with `"name": "review-my-shit"` and `"bin": {"rms": "dist/index.js"}`
- 6 updated template files using `rms` CLI calls
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current package.json bin and name -->
From package.json:
```json
{
  "name": "rms",
  "bin": { "rms": "dist/index.js" },
  "files": ["dist/"]
}
```
After change: `"name": "review-my-shit"` only — bin stays `"rms"`.

<!-- Current template patterns (all 6 use local paths) -->
opencode-review.md:    `!node dist/index.js review $ARGUMENTS`
opencode-rms-fix.md:   `!node dist/index.js fix $ARGUMENTS`
opencode-settings.md:  `!node dist/index.js settings $ARGUMENTS`
cursor-review.md:      `node dist/index.js review $ARGUMENTS`
cursor-rms-fix.md:     `node dist/index.js fix $ARGUMENTS`
cursor-settings.md:    `node dist/index.js settings $ARGUMENTS`

<!-- Target (global install convention — rms binary in PATH) -->
opencode-review.md:    `!rms review $ARGUMENTS`
opencode-rms-fix.md:   `!rms fix $ARGUMENTS`
opencode-settings.md:  `!rms settings $ARGUMENTS`
cursor-review.md:      `rms review $ARGUMENTS`
cursor-rms-fix.md:     `rms fix $ARGUMENTS`
cursor-settings.md:    `rms settings $ARGUMENTS`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename npm package and update CLI templates</name>
  <files>package.json, src/templates/opencode-review.md, src/templates/opencode-rms-fix.md, src/templates/opencode-settings.md, src/templates/cursor-review.md, src/templates/cursor-rms-fix.md, src/templates/cursor-settings.md</files>
  <read_first>
    - package.json — see current name, bin, version fields
    - src/templates/opencode-review.md — current: `!node dist/index.js review $ARGUMENTS`
    - src/templates/opencode-rms-fix.md — current: `!node dist/index.js fix $ARGUMENTS`
    - src/templates/opencode-settings.md — current: `!node dist/index.js settings $ARGUMENTS`
    - src/templates/cursor-review.md — current: `node dist/index.js review $ARGUMENTS`
    - src/templates/cursor-rms-fix.md — current: `node dist/index.js fix $ARGUMENTS`
    - src/templates/cursor-settings.md — current: `node dist/index.js settings $ARGUMENTS`
  </read_first>
  <action>
**1. package.json — rename package only:**
Change `"name": "rms"` → `"name": "review-my-shit"`.
Keep all other fields unchanged:
- `"bin": { "rms": "dist/index.js" }` — the CLI command stays `rms` ✓
- `"version": "0.1.0"` — unchanged (bump happens in plan 03)
- `"files": ["dist/"]` — unchanged

**2. OpenCode templates — replace `!node dist/index.js` with `!rms`:**

`src/templates/opencode-review.md`:
Replace the command line `!node dist/index.js review $ARGUMENTS` with `!rms review $ARGUMENTS`.
All other content (frontmatter, description, argument-hint, subtask: true) stays unchanged.

`src/templates/opencode-rms-fix.md`:
Replace `!node dist/index.js fix $ARGUMENTS` with `!rms fix $ARGUMENTS`.

`src/templates/opencode-settings.md`:
Replace `!node dist/index.js settings $ARGUMENTS` with `!rms settings $ARGUMENTS`.

**3. Cursor templates — replace `node dist/index.js` with `rms`:**

`src/templates/cursor-review.md`:
Replace all occurrences of `node dist/index.js` with `rms`:
- Main command line: `node dist/index.js review $ARGUMENTS` → `rms review $ARGUMENTS`
- Scope re-invocation: `node dist/index.js review local` → `rms review local`
- Scope re-invocation: `node dist/index.js review pr <number>` → `rms review pr <number>`
- Remove the "If the command fails with 'Cannot find module'... run `npm run build` first" note — not applicable for global install
- Keep all other prose (scope prompt handling, PR auth guidance) unchanged

`src/templates/cursor-rms-fix.md`:
Replace `node dist/index.js fix $ARGUMENTS` with `rms fix $ARGUMENTS`.
Keep all other content unchanged.

`src/templates/cursor-settings.md`:
Replace all `node dist/index.js settings` occurrences with `rms settings`:
- Main invocation line
- Example commands showing per-agent config
- Remove "If the command fails with 'Cannot find module'... run `npm run build` first" note
Keep all other content unchanged.
  </action>
  <verify>
    <automated>
      grep '"name"' package.json | grep 'review-my-shit' && echo "PASS: package name correct" || echo "FAIL: package name wrong"
      grep '"rms"' package.json | grep 'dist/index.js' && echo "PASS: bin entry correct" || echo "FAIL: bin entry missing"
      grep 'rms review' src/templates/opencode-review.md | grep '^\!' && echo "PASS: opencode review uses rms" || echo "FAIL"
      grep 'rms fix' src/templates/opencode-rms-fix.md | grep '^\!' && echo "PASS: opencode fix uses rms" || echo "FAIL"
      grep 'rms settings' src/templates/opencode-settings.md | grep '^\!' && echo "PASS: opencode settings uses rms" || echo "FAIL"
      grep 'node dist' src/templates/opencode-review.md && echo "FAIL: still has node dist" || echo "PASS: no node dist in opencode-review"
      grep 'node dist' src/templates/cursor-review.md && echo "FAIL: still has node dist in cursor-review" || echo "PASS: cursor-review clean"
      grep 'node dist' src/templates/cursor-rms-fix.md && echo "FAIL: still has node dist in cursor-fix" || echo "PASS: cursor-fix clean"
      grep 'node dist' src/templates/cursor-settings.md && echo "FAIL: still has node dist in cursor-settings" || echo "PASS: cursor-settings clean"
    </automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "review-my-shit"` (not `"rms"`)
    - package.json contains `"rms": "dist/index.js"` in the bin field (unchanged)
    - opencode-review.md contains `!rms review` (not `!node dist/index.js review`)
    - opencode-rms-fix.md contains `!rms fix` (not `!node dist/index.js fix`)
    - opencode-settings.md contains `!rms settings` (not `!node dist/index.js settings`)
    - cursor-review.md contains `rms review` and no `node dist/index.js`
    - cursor-rms-fix.md contains `rms fix` and no `node dist/index.js`
    - cursor-settings.md contains `rms settings` and no `node dist/index.js`
    - No "Cannot find module... run npm run build" notes remain in Cursor templates
    - `npm run build` exits 0 (TypeScript compiles cleanly)
  </acceptance_criteria>
  <done>All 7 files updated. Templates call `rms` binary. Package named `review-my-shit`. Build passes.</done>
</task>

</tasks>

<verification>
After task completes:

```bash
# Package name check
node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('name:', p.name, '| bin:', JSON.stringify(p.bin))"
# Expected: name: review-my-shit | bin: {"rms":"dist/index.js"}

# Template content verification
grep -l 'node dist' src/templates/*.md && echo "FAIL: templates still use node dist" || echo "PASS: all templates updated"

# Build check
npm run build
```
</verification>

<success_criteria>
- package.json `name` is `review-my-shit`
- package.json `bin.rms` is `dist/index.js` (unchanged)
- All 6 template files call `rms <subcommand>` (not `node dist/index.js`)
- `npm run build` exits 0
- All existing tests still pass: `npm test`
</success_criteria>

<output>
After completion, create `.planning/phases/10-change-the-installation-of-this-project-to-be-global-instead-of-per-project/10-01-SUMMARY.md`
</output>
