---
phase: 19-config-schema-overhaul-variant-support-and-opencode-cursor-s
plan: 05
type: execute
wave: 4
depends_on:
  - 19-03-PLAN.md
  - 19-04-PLAN.md
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "bun run build exits 0 (TypeScript compiles, templates copied)"
    - "bun run test exits 0 — all test suites pass including config and schemas"
    - "rms settings shows both opencode and cursor sections when invoked with no flags"
    - "rms settings --reviewer 'github-copilot/claude-opus-4.6:high_thinking' --opencode saves only the opencode section"
    - "Old flat config.json at ~/.config/rms/config.json is migrated on first load without error"
  artifacts:
    - path: "dist/index.js"
      provides: "compiled CLI binary"
    - path: "dist/templates/"
      provides: "installed template files"
  key_links:
    - from: "dist/index.js"
      to: "~/.config/rms/config.json"
      via: "rms settings reads/writes new nested config"
      pattern: "opencode|cursor"
---

<objective>
Full build and test verification of Phase 19 changes, followed by a human end-to-end checkpoint: build the project, run all tests, then manually verify the new config structure works correctly in practice.

Purpose: Phase 19 touches the config schema, config loading, CLI, and 8 templates — this final gate confirms the full stack is coherent before considering the phase done.

Output: Green build + tests. Human-verified config behaviour.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-config-schema-overhaul-variant-support-and-opencode-cursor-s/19-03-SUMMARY.md
@.planning/phases/19-config-schema-overhaul-variant-support-and-opencode-cursor-s/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and test — verify all changes compile and tests pass</name>
  <files></files>
  <action>
    Run in order (stop on first failure, show error):

    1. `bun run build` — must exit 0
       - Compiles TypeScript to dist/
       - Copies src/templates/ to dist/templates/

    2. `bun run test` — must exit 0
       - All vitest suites must pass, including:
         - schemas.test.ts (new AgentModelSpec, RmsConfig, FlatRmsConfig shapes)
         - config.test.ts (migration, new defaults, resolveAgentModel variant)
         - Any other suite that imports from schemas.ts or config.ts

    If any test fails: READ the failure, identify which test, determine if it's a real regression or a test that needs to be updated (since schemas changed), fix, and re-run. Do NOT skip or comment out failing tests.

    If build fails due to TypeScript errors in files NOT modified in this phase (e.g. model-picker.ts which imports AgentModelSpec with old shape): fix the TypeScript error in those files to match the new AgentModelSpec shape. Document each fix in the summary.
  </action>
  <verify>
    <automated>cd /Users/tylercrawford/dev/github/review-my-shit && bun run build && bun run test 2>&1 | tail -30</automated>
  </verify>
  <done>Build exits 0; all tests pass; no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end verification of Phase 19 config changes</name>
  <action>Verify the new config shape works correctly end-to-end in a live environment.</action>
  <verify>Human confirms all 5 verification steps pass.</verify>
  <done>Human types "approved".</done>
  <what-built>
    Complete Phase 19 config schema overhaul:
    - New RmsConfig shape with opencode + cursor sections
    - Migration from flat config on load
    - resolveAgentModel accepts variant for thinking params
    - rms settings shows both editor sections; --opencode/--cursor scope flags
    - reviewer/validator/writer subcommands accept model[:variant] arg
    - All 8 picker templates updated with new spec format
  </what-built>
  <how-to-verify>
    1. **View current config structure:**
       Run: `rms settings`
       Expected: Shows both `opencode:` and `cursor:` sections with model + [variant] display.

    2. **Set opencode reviewer only:**
       Run: `rms settings --reviewer "github-copilot/claude-sonnet-4.6:high_thinking" --opencode`
       Then: `rms settings`
       Expected: opencode reviewer shows `github-copilot/claude-sonnet-4.6 [high_thinking]`; cursor reviewer is unchanged.

    3. **Set cursor validator only:**
       Run: `rms settings --validator "gpt-5.4-high" --cursor`
       Then: `rms settings`
       Expected: cursor validator shows `gpt-5.4-high`; opencode validator is unchanged.

    4. **Migration test (if you have an old flat config):**
       Temporarily replace `~/.config/rms/config.json` with the old flat shape:
       `{"reviewer":{"provider":"copilot","model":"claude-opus-4-5"},"validator":{"provider":"copilot","model":"github-copilot/gpt-5.4"},"writer":{"provider":"copilot","model":"github-copilot/claude-haiku-4.5"}}`
       Run: `rms settings`
       Expected: No crash — shows migrated config with claude-opus-4-5 in both opencode and cursor sections.
       Restore the config file after testing.

    5. **Installed templates have correct spec format:**
       Run: `rms install --opencode` (installs templates)
       Check: `~/.config/opencode/command/rms-reviewer.md` contains "high_thinking" and "--opencode"
  </how-to-verify>
  <resume-signal>Type "approved" once all verifications pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment → config file | Tests use tmpdir isolation — no risk to actual ~/.config/rms/config.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | migration test step | accept | Documented as manual/reversible test step — user restores config afterward |
</threat_model>

<verification>
- `bun run build` exits 0
- `bun run test` all suites pass
- Human: `rms settings` shows opencode + cursor sections
- Human: scope flags work correctly (--opencode / --cursor)
</verification>

<success_criteria>
Build green, all tests pass, human verified that opencode/cursor sections display correctly and scope flags work as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/19-config-schema-overhaul-variant-support-and-opencode-cursor-s/19-05-SUMMARY.md`
</output>
