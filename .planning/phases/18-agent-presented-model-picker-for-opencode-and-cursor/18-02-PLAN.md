---
phase: 18-agent-presented-model-picker-for-opencode-and-cursor
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/cursor-rms-reviewer/SKILL.md
  - src/templates/cursor-rms-validator/SKILL.md
  - src/templates/cursor-rms-writer/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "/rms-reviewer in Cursor presents model options via agent conversation and saves the selection"
    - "/rms-validator in Cursor presents model options via agent conversation and saves the selection"
    - "/rms-writer in Cursor presents model options via agent conversation and saves the selection"
    - "All three Cursor SKILL.md files use agent-side routing (like cursor-rms-review) not raw terminal TUI"
  artifacts:
    - path: "src/templates/cursor-rms-reviewer/SKILL.md"
      provides: "Agent-presented model picker for reviewer in Cursor"
      contains: "rms settings --reviewer"
    - path: "src/templates/cursor-rms-validator/SKILL.md"
      provides: "Agent-presented model picker for validator in Cursor"
      contains: "rms settings --validator"
    - path: "src/templates/cursor-rms-writer/SKILL.md"
      provides: "Agent-presented model picker for writer in Cursor"
      contains: "rms settings --writer"
  key_links:
    - from: "cursor-rms-reviewer/SKILL.md"
      to: "rms settings --reviewer <spec>"
      via: "terminal tool call after user selects"
      pattern: "rms settings --reviewer"
---

<objective>
Rewrite the three Cursor SKILL.md model picker templates to present model choices via the agent conversation, then apply via `rms settings --<agent> <spec>`.

**Problem:** Current Cursor SKILL.md files instruct the agent to run `rms reviewer/validator/writer` in the terminal. This opens a @inquirer/prompts TUI in the terminal — which in Cursor's embedded terminal context may not work correctly or may require the user to navigate the terminal themselves rather than via the AI conversation.

**Solution:** Follow the `cursor-rms-review/SKILL.md` pattern. The agent:
1. Runs `rms settings` to read current config
2. Presents options in the conversation (tier options with model names)
3. Asks the user to choose
4. Runs `rms settings --<agent> <spec>` to apply

Purpose: Make `/rms-reviewer`, `/rms-validator`, `/rms-writer` work natively in Cursor.
Output: 3 updated SKILL.md files in `src/templates/cursor-rms-{reviewer,validator,writer}/`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key reference: how cursor-rms-review/SKILL.md handles agent-side routing -->

Pattern from src/templates/cursor-rms-review/SKILL.md (the reference to follow):

The cursor-rms-review skill has:
- Frontmatter with `name: rms-review` and a description
- Body that tells the agent to run `rms review $ARGUMENTS` in terminal
- Instructions to parse non-TTY output and present options conversationally
- Re-invocation with the chosen option

Replicate this pattern for reviewer/validator/writer but for model selection.

Tier → spec mapping to embed in Cursor SKILL.md templates:
```
max    → copilot:claude-opus-4-5        (github-copilot/claude-opus-4-5)
high   → copilot:claude-sonnet-4-5     (github-copilot/claude-sonnet-4-5)
medium → copilot:claude-haiku-3-5      (github-copilot/claude-haiku-3-5)
low    → copilot:claude-haiku-3-5      (github-copilot/claude-haiku-3-5)
```

Other providers for advanced users:
```
anthropic:claude-opus-4-5    (requires ANTHROPIC_API_KEY)
openai:gpt-4o                (requires OPENAI_API_KEY)
google:gemini-2.5-pro        (requires GOOGLE_GENERATIVE_AI_API_KEY)
```

CLI apply command:
```
rms settings --reviewer <spec>    # e.g. copilot:claude-opus-4-5
rms settings --validator <spec>
rms settings --writer <spec>
```

Custom spec formats accepted by rms settings:
- `github-copilot/model-id` (e.g. github-copilot/gpt-5.4)
- `provider:model-id` (e.g. openai:gpt-4o)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cursor-rms-reviewer/SKILL.md as agent-presented picker</name>
  <files>src/templates/cursor-rms-reviewer/SKILL.md</files>
  <read_first>
    - src/templates/cursor-rms-reviewer/SKILL.md (current: runs rms reviewer — to be replaced)
    - src/templates/cursor-rms-review/SKILL.md (reference pattern: agent-side scope selection)
  </read_first>
  <action>
Rewrite `src/templates/cursor-rms-reviewer/SKILL.md` with the following structure:

Frontmatter fields (YAML front matter at top of file):
- `name`: rms-reviewer
- `description`: "Set the reviewer agent model — interactive picker"

Body must instruct the agent to:
1. Run `rms settings` in terminal and show current config to user
2. Present the current reviewer model
3. Present these options for the user to choose from:

Tier options (Copilot provider):
- max → github-copilot/claude-opus-4-5 — Best capability, highest cost
- high → github-copilot/claude-sonnet-4-5 — High capability
- medium → github-copilot/claude-haiku-3-5 — Balanced capability and cost
- low → github-copilot/claude-haiku-3-5 — Fast and lightweight

Other providers:
- anthropic:claude-opus-4-5 — Direct Anthropic (requires ANTHROPIC_API_KEY)
- openai:gpt-4o — OpenAI GPT-4o (requires OPENAI_API_KEY)
- google:gemini-2.5-pro — Google Gemini 2.5 Pro (requires GOOGLE_GENERATIVE_AI_API_KEY)
- custom: user provides raw spec in `provider:model-id` or `github-copilot/model-id` format

4. After user chooses, map their selection to a spec:
   - max → `copilot:claude-opus-4-5`
   - high → `copilot:claude-sonnet-4-5`
   - medium or low → `copilot:claude-haiku-3-5`
   - Other options → use the spec string as-is
- Other options → use the spec string as-is

Then run in the terminal:

```
rms settings --reviewer <spec>
```

5. Run `rms settings --reviewer <spec>` in terminal to apply the selection
6. Confirm: "✓ Reviewer set to `<spec>`. Config saved to ~/.config/rms/config.json."

If `rms settings` fails, instruct the user to check that `rms` is installed (`npx review-my-shit`) and the config is valid.

The key differences from the old template:
- Does NOT run `rms reviewer` (TTY-dependent TUI)
- Presents options in the conversation and asks user to choose
- Applies the selection with `rms settings --reviewer <spec>` (no TTY needed)
  </action>
  <verify>
    <automated>grep -q "rms settings --reviewer" src/templates/cursor-rms-reviewer/SKILL.md && ! grep -q "rms reviewer$" src/templates/cursor-rms-reviewer/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/templates/cursor-rms-reviewer/SKILL.md` contains `rms settings --reviewer`
    - `src/templates/cursor-rms-reviewer/SKILL.md` does NOT contain `rms reviewer` as a standalone command (no bare `rms reviewer`)
    - The tier table (max/high/medium/low with github-copilot model names) is present
    - The spec mapping (max → copilot:claude-opus-4-5, etc.) is documented in the skill
  </acceptance_criteria>
  <done>cursor-rms-reviewer/SKILL.md presents model options in conversation and applies via rms settings --reviewer</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite cursor-rms-validator and cursor-rms-writer SKILL.md files</name>
  <files>src/templates/cursor-rms-validator/SKILL.md, src/templates/cursor-rms-writer/SKILL.md</files>
  <read_first>
    - src/templates/cursor-rms-reviewer/SKILL.md (AFTER Task 1 — use as the exact pattern)
    - src/templates/cursor-rms-validator/SKILL.md (current state — to be replaced)
    - src/templates/cursor-rms-writer/SKILL.md (current state — to be replaced)
  </read_first>
  <action>
Apply the exact same pattern from Task 1 to validator and writer SKILL.md files. Only the agent name and CLI flag differ:

**cursor-rms-validator/SKILL.md:**
- `name`: rms-validator
- `description`: "Set the validator agent model — interactive picker"
- Heading: "Present the **current validator model** from the output."
- Apply command: `rms settings --validator <spec>`
- Confirm: "✓ Validator set to `<spec>`. Config saved to ~/.config/rms/config.json."

**cursor-rms-writer/SKILL.md:**
- `name`: rms-writer
- `description`: "Set the writer agent model — interactive picker"
- Heading: "Present the **current writer model** from the output."
- Apply command: `rms settings --writer <spec>`
- Confirm: "✓ Writer set to `<spec>`. Config saved to ~/.config/rms/config.json."

Both files must have the same tier table and spec mapping as the reviewer template. Same provider options.
  </action>
  <verify>
    <automated>grep -q "rms settings --validator" src/templates/cursor-rms-validator/SKILL.md && grep -q "rms settings --writer" src/templates/cursor-rms-writer/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/templates/cursor-rms-validator/SKILL.md` contains `rms settings --validator` and the tier table
    - `src/templates/cursor-rms-writer/SKILL.md` contains `rms settings --writer` and the tier table
    - Neither file contains `rms validator` or `rms writer` as a standalone bare command
    - Both files have `name: rms-validator` and `name: rms-writer` in frontmatter respectively
  </acceptance_criteria>
  <done>All three Cursor SKILL.md model picker files use agent-presented selection, no TTY dependency</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → conversation | User states their model choice in conversation |
| agent → terminal | Agent translates choice to `rms settings --<agent> <spec>` CLI call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | custom spec via conversation | accept | `rms settings` parseSpec validates provider and model format; rejects malformed input |
| T-18-04 | Denial of Service | rms binary not found | accept | SKILL.md includes fallback guidance ("check rms is installed via npx review-my-shit") |
</threat_model>

<verification>
- All three Cursor SKILL.md files: no bare `rms reviewer/validator/writer` call, has `rms settings --<agent> <spec>`
- Tier table present with concrete github-copilot model names
- Spec mapping from tier label → provider:model string documented
</verification>

<success_criteria>
- `/rms-reviewer`, `/rms-validator`, `/rms-writer` in Cursor present choices in conversation
- User selects from table, agent applies with `rms settings --<agent> <spec>`
- No terminal TUI required — works in any Cursor context
</success_criteria>

<output>
After completion, create `.planning/phases/18-agent-presented-model-picker-for-opencode-and-cursor/18-02-SUMMARY.md`
</output>
