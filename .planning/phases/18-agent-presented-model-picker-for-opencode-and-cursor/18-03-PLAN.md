---
phase: 18-agent-presented-model-picker-for-opencode-and-cursor
plan: "03"
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - dist/templates/opencode-rms-reviewer.md
  - dist/templates/opencode-rms-validator.md
  - dist/templates/opencode-rms-writer.md
  - dist/templates/cursor-rms-reviewer/SKILL.md
  - dist/templates/cursor-rms-validator/SKILL.md
  - dist/templates/cursor-rms-writer/SKILL.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Build completes cleanly (tsc + template copy)"
    - "All 195 vitest tests still pass (no regressions)"
    - "All 6 updated command templates are installed globally"
    - "Human confirms /rms-reviewer works in their editor without 'Cancelled.' error"
  artifacts:
    - path: "dist/templates/opencode-rms-reviewer.md"
      provides: "Compiled OpenCode reviewer template"
      contains: "question: true"
    - path: "~/.config/opencode/command/rms-reviewer.md"
      provides: "Installed OpenCode command"
      contains: "question: true"
    - path: "~/.cursor/skills/rms-reviewer/SKILL.md"
      provides: "Installed Cursor skill"
      contains: "rms settings --reviewer"
  key_links:
    - from: "src/templates/"
      to: "dist/templates/"
      via: "npm run build (cp -r src/templates dist/)"
      pattern: "dist/templates/opencode-rms-reviewer.md"
    - from: "dist/templates/"
      to: "~/.config/opencode/command/"
      via: "node dist/index.js install --yes"
      pattern: "rms-reviewer.md"
---

<objective>
Build the project, run the test suite to verify no regressions, install the updated templates globally, and verify the agent-presented model picker works in the editor.

Purpose: Deliver the working commands into the user's editor and confirm they function correctly.
Output: Updated templates installed at `~/.config/opencode/command/` and `~/.cursor/skills/`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-agent-presented-model-picker-for-opencode-and-cursor/18-01-SUMMARY.md
@.planning/phases/18-agent-presented-model-picker-for-opencode-and-cursor/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and test</name>
  <files>dist/templates/opencode-rms-reviewer.md, dist/templates/opencode-rms-validator.md, dist/templates/opencode-rms-writer.md, dist/templates/cursor-rms-reviewer/SKILL.md, dist/templates/cursor-rms-validator/SKILL.md, dist/templates/cursor-rms-writer/SKILL.md</files>
  <read_first>
    - src/templates/opencode-rms-reviewer.md (verify question: true present before building)
    - src/templates/cursor-rms-reviewer/SKILL.md (verify rms settings --reviewer present)
    - package.json (build script: tsc && cp -r src/templates dist/)
  </read_first>
  <action>
1. Run the build:
   ```bash
   npm run build
   ```
   Must exit 0. The build script runs `tsc` then copies `src/templates` → `dist/templates`.

2. Verify the compiled templates contain the expected changes:
   ```bash
   grep "question: true" dist/templates/opencode-rms-reviewer.md
   grep "rms settings --reviewer" dist/templates/opencode-rms-reviewer.md
   grep "rms settings --validator" dist/templates/cursor-rms-validator/SKILL.md
   ```
   Each grep must return a match.

3. Run the full test suite:
   ```bash
   npx vitest run
   ```
   Must exit 0 with all tests passing (baseline: 195 pass from Phase 17).

4. If any test fails, read the failure output carefully. Tests do NOT check the content of OpenCode/Cursor templates (templates are not imported by tests) — so no test changes should be needed for this phase. If tests fail, it is a pre-existing issue unrelated to this change. Document any failures in the summary.
  </action>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/templates/opencode-rms-reviewer.md` exists and contains `question: true`
    - `dist/templates/opencode-rms-validator.md` exists and contains `question: true`
    - `dist/templates/opencode-rms-writer.md` exists and contains `question: true`
    - `dist/templates/cursor-rms-reviewer/SKILL.md` exists and contains `rms settings --reviewer`
    - `dist/templates/cursor-rms-validator/SKILL.md` exists and contains `rms settings --validator`
    - `dist/templates/cursor-rms-writer/SKILL.md` exists and contains `rms settings --writer`
    - `npx vitest run` exits 0 (no new failures introduced)
  </acceptance_criteria>
  <done>Build clean, templates compiled correctly, test suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Install updated templates globally</name>
  <files>~/.config/opencode/command/rms-reviewer.md, ~/.config/opencode/command/rms-validator.md, ~/.config/opencode/command/rms-writer.md, ~/.cursor/skills/rms-reviewer/SKILL.md, ~/.cursor/skills/rms-validator/SKILL.md, ~/.cursor/skills/rms-writer/SKILL.md</files>
  <read_first>
    - dist/templates/opencode-rms-reviewer.md (verify build output before installing)
  </read_first>
  <action>
Run the installer to update all 6 commands globally:

```bash
node dist/index.js install --yes
```

Must exit 0 and show the 6 commands in the completion message:
- `/rms-review`, `/rms-fix`, `/rms-reviewer`, `/rms-validator`, `/rms-writer`, `/rms-settings`

Then verify the installed files contain the new content:

```bash
grep "question: true" ~/.config/opencode/command/rms-reviewer.md
grep "rms settings --reviewer" ~/.config/opencode/command/rms-reviewer.md
grep "rms settings --validator" ~/.cursor/skills/rms-validator/SKILL.md
grep "rms settings --writer" ~/.cursor/skills/rms-writer/SKILL.md
```

Each must return a match. If any grep fails, the install did not copy the updated templates — re-run the build and install.
  </action>
  <verify>
    <automated>grep "question: true" ~/.config/opencode/command/rms-reviewer.md && grep "rms settings --reviewer" ~/.config/opencode/command/rms-reviewer.md && grep "rms settings --validator" ~/.cursor/skills/rms-validator/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.config/opencode/command/rms-reviewer.md` contains `question: true` and `rms settings --reviewer`
    - `~/.config/opencode/command/rms-validator.md` contains `question: true` and `rms settings --validator`
    - `~/.config/opencode/command/rms-writer.md` contains `question: true` and `rms settings --writer`
    - `~/.cursor/skills/rms-reviewer/SKILL.md` contains `rms settings --reviewer`
    - `~/.cursor/skills/rms-validator/SKILL.md` contains `rms settings --validator`
    - `~/.cursor/skills/rms-writer/SKILL.md` contains `rms settings --writer`
    - `node dist/index.js install --yes` exits 0
  </acceptance_criteria>
  <done>All 6 updated commands installed globally in both OpenCode and Cursor locations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — confirm agent-presented picker works in editor</name>
  <files>~/.config/opencode/command/rms-reviewer.md, ~/.cursor/skills/rms-reviewer/SKILL.md</files>
  <action>
Human verification step. No automated action needed — Task 2 completed the install.
This checkpoint verifies the end-to-end agent-presented UX works in the editor.
  </action>
  <verify>
    <automated>grep "question: true" ~/.config/opencode/command/rms-reviewer.md && echo "Templates installed with correct content"</automated>
  </verify>
  <done>Human confirms /rms-reviewer presents model choices in conversation (not "Cancelled.")</done>
  <what-built>
    Updated `/rms-reviewer`, `/rms-validator`, `/rms-writer` commands in both OpenCode and Cursor.
    These now use agent-presented model selection instead of a TTY-dependent CLI TUI.
    Previously, invoking these commands produced "Cancelled." — now the agent presents choices in conversation.
  </what-built>
  <how-to-verify>
    1. **Restart your editor** (required to pick up updated command files)
    2. In OpenCode, invoke `/rms-reviewer`
       - Expected: The agent reads current config, then presents a model selection (NOT "Cancelled.")
       - Select a tier (e.g., "high")
       - Expected: Agent runs `rms settings --reviewer copilot:claude-sonnet-4-5` and confirms
    3. Run `/rms-settings` to confirm the reviewer model changed
    4. (Optional) In Cursor, invoke `/rms-validator` and confirm similar agent-presented flow
    5. (Optional) Run `rms settings` in terminal to verify config was saved correctly
  </how-to-verify>
  <resume-signal>Type "approved" if the agent-presented picker works, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/templates/ → ~/.config/opencode/command/ | Template install via node dist/index.js install |
| dist/templates/ → ~/.cursor/skills/ | Template install via same installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | installer overwrites existing commands | accept | Installer is idempotent and the change is intentional — replacing the old TTY-dependent command with the working agent-presented version |
</threat_model>

<verification>
- `npm run build` exits 0
- `npx vitest run` exits 0 with ≥ 195 passing tests
- Installed templates contain `question: true` (OpenCode) and `rms settings --<agent>` (Cursor)
- Human confirms agent-presented picker works in their editor
</verification>

<success_criteria>
- Build clean with no TypeScript errors
- Test suite passes with no regressions
- Templates installed globally in both editors
- Human verifies `/rms-reviewer` no longer shows "Cancelled." and instead presents model choices via the agent
</success_criteria>

<output>
After completion, create `.planning/phases/18-agent-presented-model-picker-for-opencode-and-cursor/18-03-SUMMARY.md`
</output>
