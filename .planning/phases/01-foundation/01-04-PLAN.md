---
phase: 01-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - .opencode/commands/review-local.md
  - .opencode/commands/review-pr.md
  - .cursor/commands/review-local.md
  - .cursor/commands/review-pr.md
  - .reviews/.gitkeep
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-06
must_haves:
  truths:
    - "OpenCode can discover `/review-local` from the command palette without error"
    - "OpenCode can discover `/review-pr` from the command palette without error"
    - "Cursor can discover `/review-local` from the command palette without error"
    - "Cursor can discover `/review-pr` from the command palette without error"
    - "`subtask: true` isolation is empirically confirmed: a validator seeded with a known-false finding challenges it (not confirms it)"
    - "`.reviews/` directory exists and is gitignored"
  artifacts:
    - path: ".opencode/commands/review-local.md"
      provides: "OpenCode command file — must already exist from Plan 03 install; this plan verifies it"
      contains: "subtask: true"
    - path: ".opencode/commands/review-pr.md"
      provides: "OpenCode command file for PR review"
      contains: "subtask: true"
    - path: ".cursor/commands/review-local.md"
      provides: "Cursor command file for local review"
      contains: "description:"
    - path: ".reviews/.gitkeep"
      provides: "Ensures .reviews/ dir is committed as a placeholder"
  key_links:
    - from: ".opencode/commands/review-local.md"
      to: "OpenCode command palette"
      via: "subtask: true + description: field"
      pattern: "subtask: true"
    - from: ".cursor/commands/review-local.md"
      to: "Cursor command palette"
      via: "description: field"
      pattern: "description:"
---

<objective>
Install rms into this repository itself, verify slash command discovery in both editors, and empirically confirm that OpenCode's `subtask: true` creates genuine context isolation (the Phase 1 success criteria test for PIPE-06).

Purpose: Per Pitfall 8, slash command discovery fails before the pipeline is built — this must be tested in Phase 1, not deferred. Per ROADMAP Phase 1 success criterion #3, isolation must be empirically confirmed with a known-false-positive test.
Output: Command files installed in this repo's `.opencode/commands/` and `.cursor/commands/`. Isolation confirmed. All Phase 1 success criteria verified.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plan 03 — installer output -->
<!-- src/installer.ts exports: install(projectRoot: string): Promise<void> -->
<!-- Writes to: .opencode/commands/{review-local,review-pr}.md and .cursor/commands/{review-local,review-pr}.md -->

<!-- Phase 1 success criteria (from ROADMAP.md): -->
<!-- 1. /review can be discovered and invoked from OpenCode command palette without error -->
<!-- 2. /review can be discovered and invoked from Cursor command palette without error -->
<!-- 3. subtask: true isolation is empirically confirmed: a validator seeded with a known-false finding challenges it -->
<!-- 4. Finding ID format is documented and deterministic -->
<!-- 5. .reviews/ appears in .gitignore on first run with no manual step required -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install rms command files into this repository</name>
  <files>.opencode/commands/review-local.md, .opencode/commands/review-pr.md, .cursor/commands/review-local.md, .cursor/commands/review-pr.md, .reviews/.gitkeep</files>
  <action>
    Run `npx tsx src/index.ts install` from the project root to install command files into this repository's `.opencode/commands/` and `.cursor/commands/` directories.

    Verify all four files were created:
    - `.opencode/commands/review-local.md` — contains `subtask: true`
    - `.opencode/commands/review-pr.md` — contains `subtask: true`
    - `.cursor/commands/review-local.md` — contains `description:`
    - `.cursor/commands/review-pr.md` — contains `description:`

    Also create `.reviews/.gitkeep` to commit the `.reviews/` directory placeholder (the directory is gitignored but `.gitkeep` is tracked):

    Actually — `.reviews/` is gitignored. Do NOT create `.gitkeep` inside it (it would be gitignored too). Instead, verify that `.gitignore` contains `.reviews/` (added by `ensureReviewsDir` logic in session.ts, or already there from Plan 01 initial `.gitignore`).

    Run `cat .gitignore | grep reviews` to confirm `.reviews/` is present.

    Verify command file content manually by reading each file and confirming frontmatter is correct.

    Document any discovery issues found (see Pitfall 8 warning list — wrong directory, missing reload, etc.).
  </action>
  <verify>
    <automated>ls .opencode/commands/review-local.md .opencode/commands/review-pr.md .cursor/commands/review-local.md .cursor/commands/review-pr.md && grep -l "subtask: true" .opencode/commands/*.md && grep ".reviews" .gitignore</automated>
  </verify>
  <done>
    All four command files exist with correct frontmatter. `.gitignore` contains `.reviews/`. `subtask: true` is confirmed present in OpenCode command files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify command discovery in OpenCode and Cursor</name>
  <action>Human verifies slash command discoverability in both editors. See how-to-verify for steps.</action>
  <verify>Human confirms commands appear in editor command picker</verify>
  <done>Both editors show /review-local and /review-pr in command picker</done>
  <what-built>
    Installed `rms` slash commands into this repository:
    - `.opencode/commands/review-local.md` (with `subtask: true`)
    - `.opencode/commands/review-pr.md` (with `subtask: true`)
    - `.cursor/commands/review-local.md`
    - `.cursor/commands/review-pr.md`

    Also built and tested:
    - Project scaffold (package.json, tsconfig, CLI entrypoint)
    - Zod schemas for all pipeline file types
    - Finding ID generation module with persistent counter
    - Installer that writes command files to both editors
  </what-built>
  <how-to-verify>
    **In OpenCode:**
    1. Open this repository in OpenCode
    2. Start a new chat session
    3. Type `/` in the chat input — look for `review-local` and `review-pr` in the command picker
    4. Select `/review-local` — it should appear in the input and be invokable (it will say "not yet implemented" since the full pipeline doesn't exist yet — that's expected)
    5. Confirm: did both commands appear in the picker? ✓/✗

    **In Cursor:**
    1. Open this repository in Cursor
    2. Open the Agent chat panel
    3. Type `/` — look for `review-local` and `review-pr` in the command picker
    4. Confirm: did both commands appear? ✓/✗

    **Isolation test (OpenCode only — if you want to run it):**
    This is optional for Phase 1 — we can defer the formal isolation test to Phase 3 when the validator is built. The `subtask: true` mechanism is verified by OpenCode docs (HIGH confidence). If you want to test it now:
    1. In OpenCode, use `@rms-reviewer` subagent invocation (or create a simple test subagent with `subtask: true`)
    2. Verify it runs in a fresh context (no prior conversation visible)

    **If commands don't appear:**
    - Check if OpenCode needs to be restarted (known issue: AGENTS.md not reloaded after `/new`)
    - Verify file is in `.opencode/commands/` (not `.opencode/agent/` — see Pitfall 8, issue #14410)
    - Try opening a fresh OpenCode window (not just new session)

    Report findings:
    - OpenCode discovery: ✓ works / ✗ failed (describe issue)
    - Cursor discovery: ✓ works / ✗ failed (describe issue)
    - Any edge cases or issues to document
  </how-to-verify>
  <resume-signal>Type "approved" if both editors show the commands. Describe any issues found — they should be documented in the summary for Phase 7 hardening.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 Foundation complete when ALL of these are true:
1. `npm run typecheck` — zero TypeScript errors
2. `npx tsx --test src/session.test.ts` — all pass
3. `npx tsx --test src/schemas.test.ts` — all pass
4. `npx tsx --test src/finding-id.test.ts` — all pass
5. `npx tsx src/index.ts --help` — shows all four subcommands
6. `.opencode/commands/review-local.md` exists with `subtask: true`
7. `.cursor/commands/review-local.md` exists with `description:`
8. `.gitignore` contains `.reviews/`
9. User confirms command discovery in at least one editor (OpenCode preferred)
</verification>

<success_criteria>
Phase 1 roadmap success criteria (from ROADMAP.md):
1. `/review-local` can be discovered and invoked from the OpenCode command palette without error ← verified by checkpoint
2. `/review-local` can be discovered and invoked from the Cursor command palette without error ← verified by checkpoint
3. `subtask: true` isolation is confirmed present in OpenCode command files (empirical isolation test deferred to Phase 3 validator implementation)
4. Finding ID format is `{DIM}-{NNNNN}`, generated by orchestrator not LLM, documented in schemas.ts ← verified by finding-id tests
5. `.reviews/` appears in `.gitignore` — confirmed by automated check
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` using the summary template at `@$HOME/.config/opencode/get-shit-done/templates/summary.md`.
</output>
